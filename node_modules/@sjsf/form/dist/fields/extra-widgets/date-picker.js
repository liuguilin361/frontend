import { chain, fromFactories } from "../../lib/resolver.js";
export function useDatePickerForDateFormat(theme) {
    return chain(fromFactories({
        textWidget: (config) => config.schema.format === "date"
            ? theme("datePickerWidget", config)
            : undefined,
    }), theme);
}

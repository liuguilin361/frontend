export function memoize(cache, func) {
    return (arg) => {
        if (cache.has(arg)) {
            return cache.get(arg);
        }
        let ret = func(arg);
        cache.set(arg, ret);
        return ret;
    };
}
export const weakMemoize = memoize;

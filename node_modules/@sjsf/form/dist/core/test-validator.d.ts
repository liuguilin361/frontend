import type { Schema, SchemaValue } from "./schema.js";
import type { Validator } from "./validator.js";
interface TestCase {
    schema: Schema;
    value: SchemaValue;
    result: boolean;
}
export declare function createValidator({ cases, }?: {
    cases?: TestCase[];
}): Validator;
export {};

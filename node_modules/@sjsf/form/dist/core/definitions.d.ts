import { type Schema, type SchemaDefinition } from "./schema.js";
export declare function resolveRef(ref: string, rootSchema: Schema): SchemaDefinition;
export declare function findSchemaDefinition(ref: string, rootSchema: Schema, stack?: Set<string>): Schema;

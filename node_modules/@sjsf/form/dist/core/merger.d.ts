import type { Schema } from "./schema.js";
export interface Merger {
    /**
     * Merges schema and its `allOf` schemas into a single schema
     */
    mergeAllOf(schema: Schema): Schema;
}
export declare const defaultMerger: Merger;

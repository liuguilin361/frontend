import type { Merger } from './merger.js';
import { type Schema } from "./schema.js";
import type { Validator } from "./validator.js";
export declare function isFileSchema({ type, format }: Schema): boolean;
export declare function isFilesArray(validator: Validator, merger: Merger, schema: Schema, rootSchema?: Schema): boolean;

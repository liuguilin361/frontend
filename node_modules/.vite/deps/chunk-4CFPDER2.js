// node_modules/@sjsf/form/dist/lib/resolver.js
function chain(source, fallback) {
  return (type, c) => source(type, c) ?? fallback(type, c);
}
function fromRecord(record) {
  return (type) => record[type];
}
function fromFactories(factories) {
  return (type, config) => factories[type]?.(config);
}
function overrideByRecord(resolver, override) {
  return chain(fromRecord(override), resolver);
}
function extendByRecord(resolver, extension) {
  return chain(resolver, fromRecord(extension));
}

export {
  chain,
  fromRecord,
  fromFactories,
  overrideByRecord,
  extendByRecord
};
//# sourceMappingURL=chunk-4CFPDER2.js.map
